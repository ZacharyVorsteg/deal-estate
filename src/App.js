import React from 'react';
import IndustrialCRM from './IndustrialCRM';

/**
 * Root component for the Deal Estate CRM application. This component simply
 * renders the IndustrialCRM component, which contains the entire app logic.
 */
export default function App() {
  return <IndustrialCRM />;
}
